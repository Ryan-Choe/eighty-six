{
  "dataset": "eighty-six-retrieval-v1",
  "description": "Retrieval golden set: every query must surface its chunk, pinned to (source, section) metadata, at the production k=6. Pure retrieval -- no model calls in the target, so this is the one fully deterministic, zero-cost experiment. Queries probe the known failure classes: lookalike section headings across suppliers, paraphrases with no shared vocabulary, and the header-stripping regression the title re-injection fixed.",
  "examples": [
    {"inputs": {"query": "what's Roma Foods' minimum order?"},
     "outputs": {"source": "roma_foods", "section": "Minimums and fees",
       "note": "pins the header-stripping regression: this is the '$250' chunk that embedded anonymously before titles were re-injected"}},
    {"inputs": {"query": "does Cascade have an order minimum?"},
     "outputs": {"source": "cascade_produce", "section": "Minimums and fees",
       "note": "lookalike heading: Roma has an identically-named section, the k=4 failure class"}},
    {"inputs": {"query": "does Valco deliver?"},
     "outputs": {"source": "valco_cash_carry", "section": "Pickup only",
       "note": "the demo's planted trap chunk -- if this ever stops retrieving, the reorder scenario breaks"}},
    {"inputs": {"query": "when does the Roma truck come?"},
     "outputs": {"source": "roma_foods", "section": "Delivery schedule"}},
    {"inputs": {"query": "what's the order cutoff to make Roma's next truck?"},
     "outputs": {"source": "roma_foods", "section": "Delivery schedule",
       "note": "Cascade's schedule section also mentions a cutoff -- the supplier name has to discriminate"}},
    {"inputs": {"query": "how cold does the walk-in need to be?"},
     "outputs": {"source": "food_safety_storage", "section": "Cold holding",
       "note": "paraphrase: shares no number with the chunk (41F/5C)"}},
    {"inputs": {"query": "how long does opened fresh mozzarella keep?"},
     "outputs": {"source": "food_safety_storage", "section": "Fresh mozzarella"}},
    {"inputs": {"query": "can we freeze fresh mozzarella?"},
     "outputs": {"source": "food_safety_storage", "section": "Fresh mozzarella"}},
    {"inputs": {"query": "a delivery shows up warm -- what do we do?"},
     "outputs": {"source": "receiving_and_dating", "section": "At the door",
       "note": "paraphrase: 'warm' appears nowhere in the doc; the chunk says 'reject above 41F'"}},
    {"inputs": {"query": "how do use-by dates work for opened items?"},
     "outputs": {"source": "receiving_and_dating", "section": "Date labels"}},
    {"inputs": {"query": "how do I get credit from Roma for a shorted item?"},
     "outputs": {"source": "roma_foods", "section": "Shorted items and credits",
       "note": "receiving_and_dating § Shortages is the near-miss neighbor"}},
    {"inputs": {"query": "does Valco take credit cards?"},
     "outputs": {"source": "valco_cash_carry", "section": "Payment",
       "note": "lookalike heading: Roma also has a Payment section mentioning cards"}},
    {"inputs": {"query": "what's the credit policy for bad produce?"},
     "outputs": {"source": "cascade_produce", "section": "Quality credits"}}
  ]
}
