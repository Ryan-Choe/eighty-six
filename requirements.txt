# Ranges show intent; requirements.lock shows what I actually ran.
# LangChain 1.x line — pre-1.0 tutorial imports (langchain.chains etc.) don't apply here.

langchain>=1.0,<2
langchain-core>=1.0,<2
langgraph>=1.0,<2
langchain-anthropic>=1.0,<2
langsmith>=0.4

langchain-chroma>=0.2
chromadb>=1.0,<2
langchain-huggingface>=0.3
langchain-text-splitters>=1.0,<2
sentence-transformers>=3.0

streamlit>=1.45
python-dotenv>=1.0
pydantic>=2.7,<3

pytest>=8
